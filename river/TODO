Core features

  include module (multiple includes)
  Comments
  String
  Array
  Hash

Med Priority
  ! prefix operator
  "||=" operators
  short circuit "&&, ||, etc"
  unless
  return
  break
  next
  do_this "unless/if" xyz
  call "initialize" on new
  case
  elsif (?)

Core object types


Done

  nested scopes
  closures
  obj."method=" - assignment methods
