Core features

  include module (multiple includes)
  comments
  String
  Array
  Hash

Med Priority
  ! prefix operator
  "||=" operators
  short circuit "&&, ||, etc"
  obj."method=" methods
  unless
  return
  break
  next
  do_this "unless/if" xyz
  call "initialize" on new
  case
  elsif (?)

Core object types


Done

  nested scopes
  closures
