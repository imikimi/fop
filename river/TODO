Core features

  include module
  ! prefix operator
  "||="" operators
  short circuit "&&, ||, etc"
  obj."method=" methods
  unless
  return
  break
  next
  do_this "unless/if" xyz
  call "initialize" on new
  comments

Core object types

  String
  Array
  Hash

Done

  nested scopes
  closures
